# Dependency lifecycle scripts are the vector used by npm supply chain attacks, and
# nothing in this build needs them. Scripts invoked explicitly (npm run compile,
# npm run package, the vscode:prepublish hook run by vsce) are not affected.
ignore-scripts=true
